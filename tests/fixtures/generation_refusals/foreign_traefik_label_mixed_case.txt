В базовом compose проекта у сервиса 'legacy' есть метки Traefik (TRAEFIK.HTTP.ROUTERS.LEGACY.RULE, Traefik.enable): уберите их из базового compose — compose сливает метки базового файла и прод-override, а не заменяет их, поэтому чужая метка дожила бы до Traefik вне пространства имён стека.
