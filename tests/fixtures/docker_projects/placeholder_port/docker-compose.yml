name: placeholder-port-fixture

services:
  web:
    image: nginx:1.27
    ports:
      - "${DEV_PORT}:3000"
      - "8080:80"

  api:
    image: nginx:1.27
    ports:
      - target: 3000
        published: "${API_PORT}"
        protocol: tcp
